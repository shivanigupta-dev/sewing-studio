# Synthetic examples

Everything in this directory must be fictional and public-safe. Examples should demonstrate structure without reproducing a real person’s measurements, project notes, photos, filenames, or directory layout.

`sample-project.md` shows the shape of a human-readable export. Generate actual exports from the application so envelope timestamps and record IDs remain authoritative.
