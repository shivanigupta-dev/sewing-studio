# Local development requires no secrets.
#
# Put account-specific values in an untracked .env.local only when a service
# explicitly requires them. Never commit tokens, database ids, exported
# measurements, or backup contents.
